# Copy this file to .env and fill in one key. .env is gitignored.
#
# Gemini is the configured default. Either key works, and when both are set
# --provider gemini | anthropic picks between them.
GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
